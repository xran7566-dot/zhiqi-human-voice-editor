---
name: zhiqi-human-voice-editor
description: >
  中文去 AI 味写作与改编工作坊。用于处理公众号文章、短视频口播稿、小红书文案、视频脚本和参考文案：先确认采用用户个人表达、原稿作者表达，还是中性表达，再执行一次主改写，并可用个人风格 DNA 校准和 AI 痕迹扫描验收。适用于用户说去 AI 味、改得像真人、保留我的口吻、保留原作者表达、参考改写、风格提炼或检查 AI 痕迹时。
---

# 智企-去 AI 味写作工坊

统一调度三个已安装并随包提供的上游 Skill：`vendor/humanizer-zh`、`vendor/nuwa-skill` 和 `vendor/qu-ai-wei`。本 Skill 不自行捏造替代规则；只负责判断模式、调用正确的上游能力、控制顺序和汇总结果。它不是三个独立改写器的并行串联：只允许一次主改写，扫描器默认只报告问题、不自动重写。

## 1. 先确认表达来源

开始处理前，先问用户：

> 这次改写希望采用哪种表达方式？
> 1. 参考你的个人表达
> 2. 保留并参考原稿文案的表达方式
> 3. 两者都不采用，使用中性自然表达

路由规则：

- **用户个人表达**：读取用户提供或已确认的个人风格 DNA。
- **原稿作者表达**：保留原稿的语气、节奏和叙述方式，主要做润色、压缩、结构和平台适配；不擅自改成用户口吻。
- **中性自然表达**：不套用任何一方的个人风格，使用清楚、自然、不过度营销的中文。
- 用户没有选择时，不猜测；先询问一次。用户仍不指定时，使用中性自然表达并说明。

如果输入是他人文案，还要确认用户是要“保留原作者表达”还是“提炼后重新创作”。只想借鉴时，提取主题、结构、钩子和信息关系，重新组织句子，不复制原文独特表达。

## 2. 统一处理流程

### A. 用户原稿

1. 读取用户个人风格 DNA（若用户选择个人表达）。
2. 在同一轮主改写中，同时执行中文自然化和去 AI 味。
3. 把 DNA 当作保护边界：保留观点、口语、节奏、常用词和情绪，不把稿子改成统一的模型腔。
4. 运行扫描验收，检查是否仍有 AI 痕迹或改过头。

### B. 参考文案

1. 识别主题、目标读者、钩子、结构、事实和情绪推进。
2. 按用户选择保留原作者表达，或提炼后重新创作。
3. 若选择用户个人表达，再读取用户 DNA；若未选择，使用中性自然表达。
4. 一次完成改写和去 AI 味，再运行扫描验收。

## 3. 三个上游 Skill 的职责与调用方式

- **`vendor/humanizer-zh`**：唯一的主改写器。处理翻译腔、机械排比、空泛大词、结构腔、排版和中文节奏。调用前读取其 `SKILL.md`，需要深度模式时按其规则读取对应 `references/`。
- **`vendor/nuwa-skill`**：只在用户需要建立或更新个人风格 DNA 时调用。它负责从用户提供并确认的样本提炼可运行的风格/表达档案；没有样本时不得虚构个人风格，改用中性自然表达。普通改稿不要把 Nuwa 当第二个全文改写器。
- **`vendor/qu-ai-wei`**：作为扫描与边界检查能力。调用时要求检查事实、原意、证据强度、语体、作者声口和 AI 高频结构；默认输出问题清单，不直接覆盖主改稿。只有用户明确确认扫描结果后，才做局部二次修改，并保留主改稿。

三个上游 Skill 的完整内容位于 `vendor/`，不是本 Skill 自己重新编写的替代品。更新上游版本时，必须同步其许可证、来源和版本记录。

不得让两个模块轮流完整重写同一篇正文。需要二次修改时，先展示扫描结果，再按用户确认的项目局部修改。

## 4. 输出格式

默认输出：

1. **表达方式**：用户个人表达 / 原稿作者表达 / 中性自然表达；
2. **处理说明**：保留了什么、改动了什么；
3. **最终稿**；
4. **扫描结果**：通过项、残留问题、待用户确认项；
5. **版权与来源提醒**：参考他人文案时，保留来源信息；对外发布前由用户确认使用权。

重要稿件可另附“原稿—主改稿—扫描报告—最终稿”，便于回溯。

## 5. 约束

- 不把“去 AI 味”理解成统一口语化；技术、公文、学术和正式文本保留必要的正式程度。
- 不在没有读取上游 Skill 的情况下自行发明一套规则替代它们。
- 不编造用户经历、观点、数据或个人习惯。
- 不把原作者的独特表达改几个词后冒充原创；需要改编时必须重新组织表达。
- 不默认套用任何作者声音档案；只有用户明确选择并有合法使用依据时才参考。
- 个人风格 DNA 必须来自用户确认的样本，而不是模型猜测。
- 扫描器默认不自动改正文，避免与主改写器打架。

## 6. 参考来源

本 Skill 的中文自然化规则参考公开的 `ai-zixun/humanizer-zh`；其具体文件、版本和许可证以原仓库为准。本 Skill 不依赖外部 API，不复制上游仓库代码；如后续吸收具体文本或规则，必须在发布版本中保留来源与许可证说明，详见 `references/attribution.md`。
